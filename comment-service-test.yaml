  
server:
  port: 6868

spring:
  application:
    name: comment-service-test
  datasource:
    url: jdbc:h2:file:./commentsTestDB
    driverClassName: org.h2.Driver
    username: sa
    password: password
    initialization-mode: always
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
